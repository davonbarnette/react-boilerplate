import React, {Component} from 'react';
import {observer} from "mobx-react";

import './styles.scss';

import {{pascalCase componentName}}Form from "./Form";
import AppActions from "../../../../data/App/Actions";
import {FIXED_COMPONENT_TYPES} from "../../../../data/App/Types";
import Modal from "../../../common/Modal/Modal";
import {Flex} from "../../../common/Flex/Flex";

class {{pascalCase componentName}}FormModal extends Component{

    onExitModal = () => {
        AppActions.closeFixedComponent(FIXED_COMPONENT_TYPES.MODAL);
    };

    render(){
        return(
            <Modal exitModalFn={this.onExitModal} title='Add {{pascalCase componentName}}'>
                <Flex className='{{snakeCase componentName}}-modal-content' flexDirection='column' justifyContent='center'>
                    <{{pascalCase componentName}}Form/>
                </Flex>
            </Modal>
        )
    }
}

export default observer({{pascalCase componentName}}FormModal);